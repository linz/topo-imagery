from pathlib import Path

DATA_DIR = Path(__file__).parent / "data"
"""Fixtures for this package's tests, resolved relative to this file rather than the working
directory so the suite can be run from anywhere."""

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"

[project]
name = "topo-imagery"
version = "8.0.0"
description = "A collection of scripts for processing geospatial dataset"
authors = [
    { name = "Blayne Chard", email = "bchard@linz.govt.nz" },
    { name = "Daniel Silk", email = "dsilk@linz.govt.nz" },
    { name = "Alice Fage", email = "afage@linz.govt.nz" },
    { name = "Sheetal Poudel", email = "spoudel@linz.govt.nz" },
    { name = "Tobias Schmidt", email = "tschmidt@linz.govt.nz" },
]
requires-python = "~=3.12.3"
dependencies = [
    "boto3",
    "linz-logger",
    "shapely>=2.0",
    "geoprocessor-pointcloud",
    "geoprocessor-raster",
]

[dependency-groups]
dev = [
    "black",
    "gitlint",
    "isort",
    "moto>=4.2",
    "mypy",
    "mypy-boto3-s3",
    "pre-commit",
    "pylint",
    "pytest",
    "pytest-dependency",
    "pytest-subtests",
    "shellcheck-py",
]

[tool.black]
line-length = 127

[tool.pytest.ini_options]
pythonpath = [
    "packages/geoprocessor-common/test",
    "packages/geoprocessor-gdal/test",
    "packages/geoprocessor-pdal/test",
    "packages/geoprocessor-stac/test",
]

[tool.uv]
package = false
default-groups = "all"

[tool.uv.sources]
geoprocessor-pointcloud = { workspace = true }
geoprocessor-raster = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]

[tool.isort]
line_length = 127
case_sensitive = true
profile = "black"

[tool.mypy]
show_error_codes = true
strict = true
disable_error_code = ["import"]
exclude = ["conftest\\.py$"]

[[tool.mypy.overrides]]
module = ["linz_logger"]
ignore_missing_imports = true

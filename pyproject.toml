[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "topo-data-processing"
version = "0.1.0"
description = ""
authors = ["Paul Fouquet <pfouquet@linz.govt.nz>"]
readme = "README.md"
packages = [{include = "topo_data_processing"}]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]
black = "^23.1.0"
isort = "^5.12.0"
gitlint = "^0.18.0"
mypy = "^1.0"
pylint = "^2.16.1"
pre-commit = "^2.21.0"
